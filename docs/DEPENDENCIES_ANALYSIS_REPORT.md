# Отчет по анализу зависимостей проекта Tilda Update Checker

**Дата анализа:** 28 декабря 2025  
**Версия Python:** 3.13  
**Анализ через:** Context7 (официальная документация)

---

## Исполнительное резюме

Проект использует **9 основных зависимостей**. Проведен детальный анализ через официальную документацию (Context7). Обнаружены следующие проблемы:

- 🔴 **КРИТИЧЕСКИЕ:** 2 проблемы
- 🟡 **ВАЖНЫЕ:** 3 проблемы
- 🟢 **РЕКОМЕНДАЦИИ:** 4 улучшения

---

## 1. Критически важные зависимости

### 1.1 requests (>=2.31.0) 🟢

**Текущая установленная версия:** 2.32.5  
**Рекомендуемая версия:** >=2.32.5  
**Статус:** ✅ Актуально

#### Анализ использования в коде

**Файл:** `src/cdn_fetcher.py`

**✅ Правильно:**

- Используется `requests.Session()` для переиспользования соединений
- Настроена retry стратегия через `HTTPAdapter` и `Retry`
- Установлен `timeout` для всех запросов (30 секунд)
- Правильная обработка исключений `requests.exceptions.RequestException`

**⚠️ Рекомендации:**

1. **Раздельные таймауты:** Рекомендуется использовать кортеж `(connect_timeout, read_timeout)` вместо одного значения:

   ```python
   # Текущий код (строка 155)
   response = self.session.get(url, timeout=config.REQUEST_TIMEOUT)

   # Рекомендуется
   response = self.session.get(url, timeout=(5.0, 30.0))  # (connect, read)
   ```

2. **Retry configuration:** Текущая конфигурация хороша, но можно добавить `raise_on_status=False` для лучшего контроля:
   ```python
   # Строка 36-41
   retry_strategy = Retry(
       total=config.REQUEST_RETRY_COUNT,
       backoff_factor=config.REQUEST_RETRY_DELAY,
       status_forcelist=[429, 500, 502, 503, 504],
       allowed_methods=["GET"],
       raise_on_status=False  # Добавить
   )
   ```

**Приоритет:** LOW

---

### 1.2 sqlalchemy (>=2.0.0) 🔴

**Текущая установленная версия:** 2.0.45  
**Рекомендуемая версия:** >=2.0.45  
**Статус:** ⚠️ Требует исправлений

#### Критические проблемы

**Файл:** `src/database.py`

**🔴 ПРОБЛЕМА 1: Устаревший стиль импорта (строка 19)**

```python
# Текущий код
from sqlalchemy.ext.declarative import declarative_base

# SQLAlchemy 2.0 стиль
from sqlalchemy.orm import declarative_base
```

**Причина:** `sqlalchemy.ext.declarative.declarative_base` deprecated в SQLAlchemy 2.0. Новый импорт из `sqlalchemy.orm`.

**🔴 ПРОБЛЕМА 2: Отсутствие context manager для сессий**

Текущий код использует ручное управление сессиями:

```python
# Строки 266-270
session = self.get_session()
try:
    return session.query(TrackedFile).all()
finally:
    session.close()
```

**Рекомендуется SQLAlchemy 2.0 стиль:**

```python
def get_tracked_files(self) -> List[TrackedFile]:
    """Получить список всех отслеживаемых файлов"""
    with self.SessionLocal() as session:
        return session.query(TrackedFile).all()
```

**🟡 ПРОБЛЕМА 3: Устаревший sessionmaker параметр**

```python
# Строки 245-249
self.SessionLocal = sessionmaker(
    autocommit=False,  # ⚠️ Deprecated в 2.0
    autoflush=False,
    bind=self.engine
)

# SQLAlchemy 2.0 стиль
self.SessionLocal = sessionmaker(
    autoflush=False,
    bind=self.engine
)
```

**Параметр `autocommit=False` deprecated** - это поведение по умолчанию в 2.0.

**Приоритет:** CRITICAL

---

### 1.3 openai (>=1.0.0) 🟢

**Текущая установленная версия:** 2.14.0  
**Рекомендуемая версия:** >=2.14.0  
**Статус:** ✅ Хорошо

#### Анализ использования в коде

**Файл:** `src/llm_analyzer.py`

**✅ Правильно:**

- Используется современный клиент `OpenAI(api_key=...)`
- Правильная обработка исключений `OpenAIError`
- Используется `response_format={"type": "json_object"}` для структурированного вывода

**⚠️ Рекомендации:**

1. **Добавить timeout и retry конфигурацию:**

   ```python
   # Строка 95
   self.client = OpenAI(api_key=config.OPENAI_API_KEY)

   # Рекомендуется
   self.client = OpenAI(
       api_key=config.OPENAI_API_KEY,
       timeout=60.0,  # Добавить timeout
       max_retries=2   # Добавить retries
   )
   ```

2. **Использовать context manager для автоматического закрытия:**
   ```python
   async def analyze_with_context():
       async with OpenAI(api_key=config.OPENAI_API_KEY) as client:
           response = await client.chat.completions.create(...)
   ```

**Приоритет:** MEDIUM

---

## 2. Интеграционные зависимости

### 2.1 python-telegram-bot (>=20.0) 🔴

**Текущая установленная версия:** 22.5  
**Рекомендуемая версия:** >=22.5  
**Статус:** ⚠️ КРИТИЧЕСКАЯ ПРОБЛЕМА

#### Критические проблемы

**Файл:** `src/telegram_notifier.py`

**🔴 ПРОБЛЕМА: Библиотека НЕ используется!**

Код использует прямые HTTP запросы к Telegram Bot API через `requests` (строки 468-496), вместо использования установленной библиотеки `python-telegram-bot`.

```python
# Текущий код (строка 468-479)
import requests

url = f"https://api.telegram.org/bot{self.bot_token}/sendMessage"
payload = {
    "chat_id": self.chat_id,
    "text": message,
    "parse_mode": parse_mode,
    "disable_web_page_preview": True
}
response = requests.post(url, json=payload, timeout=10)
```

**Проблемы:**

1. **Зависимость не используется** - `python-telegram-bot` установлен, но не импортируется
2. **Версия 20.0+ полностью async** - требует `async/await`
3. **Отсутствует обработка rate limiting**
4. **Нет автоматических retry**

**Рекомендации:**

**Вариант A: Использовать установленную библиотеку (рекомендуется)**

```python
from telegram import Bot
from telegram.error import TelegramError
import asyncio

class TelegramNotifier:
    def __init__(self, bot_token: str = None, chat_id: str = None):
        self.bot_token = bot_token
        self.chat_id = chat_id
        self.bot = Bot(token=bot_token) if bot_token else None
        self.enabled = bool(bot_token and chat_id)

    async def send_message_async(self, message: str) -> bool:
        if not self.enabled:
            return False
        try:
            await self.bot.send_message(
                chat_id=self.chat_id,
                text=message,
                parse_mode='Markdown'
            )
            return True
        except TelegramError as e:
            logger.error(f"Telegram error: {e}")
            return False

    def send_message(self, message: str) -> bool:
        """Sync wrapper"""
        return asyncio.run(self.send_message_async(message))
```

**Вариант B: Удалить зависимость из requirements.txt**

Если вы хотите продолжать использовать прямые HTTP запросы, удалите `python-telegram-bot` из `requirements.txt`, так как он не используется и занимает место.

**Приоритет:** CRITICAL

---

### 2.2 apscheduler (>=3.10.0) 🟡

**Текущая установленная версия:** 3.11.2  
**Рекомендуемая версия:** >=4.0.0  
**Статус:** ⚠️ Устаревшая версия

#### Анализ

**Файл:** `main.py`

**✅ Правильно:**

- Используется `BlockingScheduler` для синхронного выполнения
- Правильная обработка `KeyboardInterrupt`

**🟡 ПРОБЛЕМА: APScheduler 3.x устарел**

APScheduler 4.0 вышел и является **полностью переписанной версией** с:

- Лучшей производительностью
- Современным async API
- Улучшенной типизацией
- Более простым API

**Текущий код (строки 203-231):**

```python
from apscheduler.schedulers.blocking import BlockingScheduler

scheduler = BlockingScheduler()
scheduler.add_job(
    check_and_analyze,
    'interval',
    hours=interval_hours if interval_hours > 0 else 1,
    id="tilda_checker"
)
scheduler.start()
```

**APScheduler 4.0 стиль:**

```python
from apscheduler import Scheduler
from apscheduler.triggers.interval import IntervalTrigger

with Scheduler() as scheduler:
    scheduler.add_schedule(
        check_and_analyze,
        IntervalTrigger(hours=interval_hours or 1),
        id="tilda_checker"
    )
    scheduler.run_until_stopped()
```

**Рекомендации:**

1. Обновить до APScheduler 4.x
2. Переписать код под новый API
3. Использовать context manager для автоматической очистки

**Приоритет:** MEDIUM (работает, но устарело)

---

## 3. Вспомогательные зависимости

### 3.1 beautifulsoup4 (>=4.12.0) 🟢

**Текущая установленная версия:** 4.14.3  
**Рекомендуемая версия:** >=4.14.3  
**Статус:** ✅ Актуально

**Анализ:** Библиотека не используется в текущем коде, но может быть полезна для будущего парсинга HTML страниц Tilda.

**Рекомендация:** Если не планируется парсинг HTML, можно удалить из зависимостей.

**Приоритет:** LOW

---

### 3.2 python-dotenv (>=1.0.0) 🟢

**Текущая установленная версия:** 1.2.1  
**Рекомендуемая версия:** >=1.2.1  
**Статус:** ✅ Отлично

#### Анализ использования

**Файл:** `config.py`

**✅ Правильно:**

```python
# Строка 9
from dotenv import load_dotenv
load_dotenv()
```

Использован правильный подход - загрузка в начале модуля конфигурации.

**Приоритет:** OK

---

### 3.3 pyyaml (>=6.0.0) 🟡

**Текущая установленная версия:** 6.0.3  
**Рекомендуемая версия:** >=6.0.3  
**Статус:** ⚠️ Не используется, но есть риски

**🟡 ПРОБЛЕМА: Библиотека не используется в коде**

PyYAML установлен, но нигде не импортируется. Если планируется использование:

**⚠️ ВАЖНО: Всегда используйте `yaml.safe_load()`**

```python
# ❌ ОПАСНО - позволяет выполнение произвольного кода
import yaml
data = yaml.load(file)

# ✅ БЕЗОПАСНО
import yaml
data = yaml.safe_load(file)
```

**Известные уязвимости:**

- CVE-2020-14343 (исправлено в 5.4+)
- Arbitrary code execution через `yaml.load()`

**Рекомендации:**

1. Если не используется - удалить из requirements.txt
2. Если планируется использование - ВСЕГДА использовать `safe_load()`

**Приоритет:** MEDIUM

---

### 3.4 packaging (>=23.0) 🟢

**Текущая установленная версия:** 25.0  
**Рекомендуемая версия:** >=25.0  
**Статус:** ✅ Актуально

**Анализ:** Используется для работы с версиями в модуле `src/version_detector.py`.

**Приоритет:** OK

---

## 4. Проверка конфликтов и транзитивных зависимостей

### 4.1 Транзитивные зависимости

Анализ установленных пакетов показывает следующие транзитивные зависимости:

**OpenAI:**

- `httpx` (0.28.1) - HTTP клиент
- `pydantic` (2.12.5) - валидация данных
- `pydantic-core` (2.41.5)
- `anyio` (4.12.0) - async utilities
- `sniffio` (1.3.1)
- `h11` (0.16.0)
- `httpcore` (1.0.9)

**Requests:**

- `urllib3` (2.6.2)
- `certifi` (2025.11.12)
- `charset-normalizer` (3.4.4)
- `idna` (3.11)

**SQLAlchemy:**

- Нет транзитивных зависимостей

**Python-telegram-bot:**

- `httpx` (конфликт с OpenAI - используют одну и ту же версию ✅)

### 4.2 Потенциальные конфликты

**✅ Конфликтов не обнаружено**

- `httpx` используется и OpenAI, и python-telegram-bot - версии совместимы
- Все зависимости имеют совместимые версии Python (3.8+)

### 4.3 Дублирование функционала

**⚠️ Дублирование HTTP клиентов:**

1. `requests` - используется в `cdn_fetcher.py` и `telegram_notifier.py`
2. `httpx` - установлен как зависимость OpenAI, но не используется напрямую

**Рекомендация:** Оставить как есть. `requests` - синхронный, `httpx` - async. Разные use cases.

---

## 5. Сводная таблица статуса зависимостей

| Зависимость         | Текущая | Рекомендуемая | Статус               | Приоритет    |
| ------------------- | ------- | ------------- | -------------------- | ------------ |
| requests            | 2.32.5  | >=2.32.5      | 🟢 OK                | LOW          |
| beautifulsoup4      | 4.14.3  | >=4.14.3      | 🟢 OK                | LOW          |
| openai              | 2.14.0  | >=2.14.0      | 🟢 OK                | MEDIUM       |
| apscheduler         | 3.11.2  | >=4.0.0       | 🟡 Устарело          | MEDIUM       |
| sqlalchemy          | 2.0.45  | >=2.0.45      | 🔴 Нужны исправления | **CRITICAL** |
| python-dotenv       | 1.2.1   | >=1.2.1       | 🟢 OK                | OK           |
| pyyaml              | 6.0.3   | >=6.0.3       | 🟡 Не используется   | MEDIUM       |
| packaging           | 25.0    | >=25.0        | 🟢 OK                | OK           |
| python-telegram-bot | 22.5    | >=22.5        | 🔴 Не используется!  | **CRITICAL** |

---

## 6. План действий (приоритизированный)

### 🔴 КРИТИЧЕСКИЕ (выполнить немедленно)

#### 1. Исправить SQLAlchemy 2.0 deprecations

**Файл:** `src/database.py`

```python
# Изменить строку 19
from sqlalchemy.orm import declarative_base  # вместо ext.declarative

# Изменить строки 245-249
self.SessionLocal = sessionmaker(
    autoflush=False,  # удалить autocommit=False
    bind=self.engine
)

# Переписать все методы с ручным управлением сессиями на context manager
def get_tracked_files(self) -> List[TrackedFile]:
    with self.SessionLocal() as session:
        return session.query(TrackedFile).all()
```

**Оценка времени:** 2-3 часа  
**Риск:** Средний (нужно тестирование)

#### 2. Решить проблему с python-telegram-bot

**Вариант A (рекомендуется):** Переписать `TelegramNotifier` на использование библиотеки

```python
# Полностью переписать src/telegram_notifier.py
from telegram import Bot
from telegram.error import TelegramError
import asyncio

class TelegramNotifier:
    def __init__(self, bot_token: str = None, chat_id: str = None):
        self.bot = Bot(token=bot_token) if bot_token else None
        # ... остальной код
```

**Оценка времени:** 4-6 часов  
**Риск:** Высокий (требует тестирования async кода)

**Вариант B (быстрое решение):** Удалить из requirements.txt

```txt
# Удалить строку
python-telegram-bot>=20.0
```

**Оценка времени:** 5 минут  
**Риск:** Нет

### 🟡 ВАЖНЫЕ (выполнить в ближайшее время)

#### 3. Обновить APScheduler до 4.x

```txt
# requirements.txt
apscheduler>=4.0.0  # вместо >=3.10.0
```

Переписать код в `main.py` под новый API.

**Оценка времени:** 3-4 часа  
**Риск:** Средний

#### 4. Добавить timeout и retry для OpenAI клиента

```python
# src/llm_analyzer.py, строка 95
self.client = OpenAI(
    api_key=config.OPENAI_API_KEY,
    timeout=60.0,
    max_retries=2
)
```

**Оценка времени:** 15 минут  
**Риск:** Низкий

#### 5. Удалить неиспользуемые зависимости

Если `pyyaml` и `beautifulsoup4` не используются:

```txt
# Удалить из requirements.txt
# beautifulsoup4>=4.12.0
# pyyaml>=6.0.0
```

**Оценка времени:** 5 минут  
**Риск:** Нет

### 🟢 РЕКОМЕНДАЦИИ (улучшения)

#### 6. Улучшить timeout конфигурацию для requests

```python
# src/cdn_fetcher.py, строка 155
response = self.session.get(
    url,
    timeout=(5.0, 30.0)  # (connect, read)
)
```

**Оценка времени:** 10 минут  
**Риск:** Низкий

#### 7. Добавить type hints для всех функций

Использовать `mypy` для проверки типов.

**Оценка времени:** 2-3 часа  
**Риск:** Нет

---

## 7. Обновленный requirements.txt (рекомендуемый)

```txt
# HTTP клиент
requests>=2.32.5

# OpenAI API
openai>=2.14.0

# Планировщик задач
apscheduler>=4.0.0

# ORM для базы данных
sqlalchemy>=2.0.45

# Переменные окружения
python-dotenv>=1.2.1

# Работа с версиями пакетов
packaging>=25.0

# Telegram Bot (если используется)
# python-telegram-bot>=22.5  # Удалить, если не используется

# HTML парсинг (если используется)
# beautifulsoup4>=4.14.3  # Удалить, если не используется

# YAML парсинг (если используется)
# pyyaml>=6.0.3  # Удалить, если не используется
```

---

## 8. Заключение

### Общая оценка: 🟡 ТРЕБУЮТСЯ УЛУЧШЕНИЯ

**Сильные стороны:**

- ✅ Все зависимости актуальных версий
- ✅ Нет критических уязвимостей безопасности
- ✅ Нет конфликтов между зависимостями
- ✅ Правильное использование requests и python-dotenv

**Критические проблемы:**

- 🔴 SQLAlchemy использует deprecated API
- 🔴 python-telegram-bot установлен, но не используется
- 🟡 APScheduler 3.x устарел (вышел 4.0)
- 🟡 Неиспользуемые зависимости (pyyaml, beautifulsoup4)

### Рекомендуемый порядок действий:

1. **Немедленно:** Исправить SQLAlchemy deprecations (2-3 часа)
2. **Немедленно:** Решить проблему с python-telegram-bot (удалить или использовать)
3. **На этой неделе:** Обновить APScheduler до 4.x
4. **На этой неделе:** Добавить timeout/retry для OpenAI
5. **Когда удобно:** Удалить неиспользуемые зависимости
6. **Когда удобно:** Улучшить timeout конфигурацию requests

### Оценка трудозатрат:

- **Критические исправления:** 6-9 часов
- **Важные улучшения:** 4-5 часов
- **Рекомендации:** 2-3 часа
- **Итого:** 12-17 часов работы

---

**Отчет подготовлен:** 28 декабря 2025  
**Анализ выполнен через:** Context7 (официальная документация)  
**Методология:** Статический анализ кода + проверка best practices + анализ документации
