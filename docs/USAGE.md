# Руководство по использованию Tilda Update Checker

## Быстрый старт

### 1. Настройка окружения

```bash
# Перейти в директорию проекта
cd /Users/mac/Documents/tilda-update-checker

# Активировать виртуальное окружение
source venv/bin/activate

# (Опционально) Настроить OpenAI API ключ
cp env.example .env
# Отредактировать .env и добавить OPENAI_API_KEY=your-key-here
```

### 2. Первый запуск (создание baseline)

```bash
python main.py --once
```

**Что происходит:**
- ✅ Создается база данных SQLite в `data/tilda_checker.db`
- ✅ Загружаются все файлы из списка мониторинга
- ✅ Вычисляются SHA-256 хеши для каждого файла
- ✅ Данные сохраняются как baseline (базовое состояние)
- ℹ️ При первом запуске изменений не будет

### 3. Повторные проверки

```bash
# Однократная проверка
python main.py --once

# Запуск в режиме демона (проверка каждый час)
python main.py --daemon
```

**Что происходит:**
- 🔍 Загружаются файлы с CDN
- 🔐 Вычисляются хеши
- 🆚 Сравниваются с сохраненными хешами
- 📊 Если найдены изменения → анализ через LLM
- 📝 Генерация и сохранение анонсов

### 4. Просмотр анонсов

```bash
# Показать последние 10 анонсов
python main.py --show-announcements

# Показать последние 5 анонсов
python main.py --show-announcements -n 5
```

## Структура проекта

```
tilda-update-checker/
├── data/
│   └── tilda_checker.db     # SQLite база данных
├── logs/
│   └── tilda_checker.log    # Логи приложения
├── src/
│   ├── cdn_fetcher.py       # Загрузка файлов с CDN
│   ├── diff_detector.py     # Обнаружение изменений
│   ├── llm_analyzer.py      # Анализ через OpenAI
│   ├── announcement.py      # Генерация анонсов
│   └── database.py          # Работа с SQLite
├── venv/                    # Виртуальное окружение
├── config.py                # Конфигурация
├── main.py                  # Точка входа
└── requirements.txt         # Зависимости
```

## Конфигурация

Основные параметры в `config.py`:

- `TILDA_CORE_FILES` - список отслеживаемых файлов Tilda CDN
- `TILDA_CHECK_INTERVAL` - интервал проверки (секунды)
- `OPENAI_MODEL` - модель OpenAI (по умолчанию: gpt-4o-mini)
- `MAX_DIFF_TOKENS` - макс. токенов для LLM (2000)

## Режимы работы

### 1. Режим `--once` (однократная проверка)

**Использование:**
```bash
python main.py --once
```

**Когда использовать:**
- Первый запуск (создание baseline)
- Тестирование
- Ручная проверка изменений
- Отладка

**Процесс:**
1. Инициализация БД
2. Загрузка файлов
3. Проверка изменений
4. LLM анализ (если есть изменения)
5. Сохранение анонсов
6. Завершение

### 2. Режим `--daemon` (фоновый режим)

**Использование:**
```bash
python main.py --daemon
```

**Когда использовать:**
- Постоянный мониторинг
- Продакшн окружение
- Автоматическое отслеживание

**Процесс:**
1. Инициализация БД
2. Первая проверка (сразу)
3. Запуск планировщика
4. Проверка каждый час (по умолчанию)
5. Цикл продолжается до Ctrl+C

**Остановка:**
```
Нажмите Ctrl+C для остановки
```

### 3. Режим `--show-announcements` (просмотр анонсов)

**Использование:**
```bash
python main.py --show-announcements
python main.py --show-announcements -n 20
```

**Что показывает:**
- Последние анонсы из БД
- Дата и время генерации
- Уровень важности (🔴 критическое, 🟡 важное, 🟢 незначительное)
- Описание изменений
- Рекомендации

## Логирование

### Уровни логов

- `INFO` - общая информация о работе
- `WARNING` - предупреждения (например, 404 ошибки)
- `ERROR` - ошибки (с продолжением работы)
- `DEBUG` - детальная отладочная информация

### Файлы логов

**Расположение:** `logs/tilda_checker.log`

**Просмотр:**
```bash
# Последние 50 строк
tail -50 logs/tilda_checker.log

# Следить за логами в реальном времени
tail -f logs/tilda_checker.log

# Поиск ошибок
grep ERROR logs/tilda_checker.log
```

## Работа с OpenAI API

### Настройка ключа

**Способ 1: Через .env файл**
```bash
cp env.example .env
nano .env  # Добавить OPENAI_API_KEY=sk-...
```

**Способ 2: Через переменную окружения**
```bash
export OPENAI_API_KEY="sk-..."
python main.py --once
```

### Без OpenAI API

Приложение работает и без ключа OpenAI:
- ✅ Обнаружение изменений работает
- ✅ Сохранение в БД работает
- ⚠️ Анализ будет базовым (без LLM)
- ⚠️ Анонсы создаются по простым эвристикам

### Стоимость

**Модель:** gpt-4o-mini
- Input: $0.150 за 1M токенов
- Output: $0.600 за 1M токенов

**Средний анализ:**
- ~2000 input токенов
- ~500 output токенов
- **Стоимость: ~$0.0006 за анализ**

**Месячная стоимость:**
- При 1 изменении в день: ~$0.018/месяц
- При 10 изменениях в день: ~$0.18/месяц

## Мониторируемые файлы

По умолчанию отслеживаются:

**JavaScript файлы:**
- tilda-fallback-1.0.min.js
- tilda-scripts-3.0.min.js
- tilda-cover-1.0.min.js
- tilda-animation-1.0.min.js
- tilda-zero-1.1.min.js
- tilda-forms-1.0.min.js
- tilda-menu-1.0.min.js

**CSS файлы:**
- tilda-grid-3.0.min.css

## Устранение проблем

### Проблема: База данных не создается

**Решение:**
```bash
# Создать директорию data вручную
mkdir -p data
chmod 755 data
python main.py --once
```

### Проблема: Ошибки импорта модулей

**Решение:**
```bash
# Переустановить зависимости
source venv/bin/activate
pip install -r requirements.txt --force-reinstall
```

### Проблема: 404 ошибки при загрузке файлов

**Причина:** Файл больше не существует или изменился URL

**Решение:**
1. Проверить актуальность URL на сайте Tilda
2. Обновить список в `config.py` → `TILDA_CORE_FILES`
3. Перезапустить проверку

### Проблема: OpenAI API ошибки

**Возможные причины:**
- Неверный API ключ
- Недостаточно средств на счету
- Превышен rate limit

**Решение:**
1. Проверить ключ в .env
2. Проверить баланс на platform.openai.com
3. При необходимости увеличить интервал проверки

## Системные требования

- Python 3.11+
- ~50MB дискового пространства
- 256MB RAM (минимум)
- Интернет соединение

## Автозапуск (Linux/Mac)

### Через systemd (Linux)

**Создать файл сервиса:**
```bash
sudo nano /etc/systemd/system/tilda-checker.service
```

**Содержимое:**
```ini
[Unit]
Description=Tilda Update Checker
After=network.target

[Service]
Type=simple
User=your-user
WorkingDirectory=/path/to/tilda-update-checker
ExecStart=/path/to/tilda-update-checker/venv/bin/python main.py --daemon
Restart=always

[Install]
WantedBy=multi-user.target
```

**Запустить:**
```bash
sudo systemctl enable tilda-checker
sudo systemctl start tilda-checker
sudo systemctl status tilda-checker
```

### Через cron (Mac/Linux)

```bash
crontab -e
```

**Добавить:**
```
0 * * * * cd /path/to/tilda-update-checker && ./venv/bin/python main.py --once
```

## Дальнейшее развитие

**Планируемые возможности:**
- Telegram интеграция
- Веб-интерфейс
- Email уведомления
- Diff-viewer
- Автообнаружение файлов

**Вклад в проект приветствуется!**



