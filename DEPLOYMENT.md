# 🚀 Инструкция по деплою через Dokploy

Это пошаговое руководство по развертыванию **Tilda Update Checker** на сервере Ubuntu 24 с использованием [Dokploy](https://dokploy.com/).

## 📋 Содержание

- [Преимущества Dokploy](#преимущества-dokploy)
- [Подготовка сервера](#1-подготовка-сервера)
- [Установка Dokploy](#2-установка-dokploy)
- [Подготовка репозитория](#3-подготовка-репозитория)
- [Настройка приложения в Dokploy](#4-настройка-приложения-в-dokploy)
- [Конфигурация volumes](#5-конфигурация-volumes)
- [Environment Variables](#6-environment-variables)
- [Автоматический деплой](#7-автоматический-деплой)
- [Проверка и мониторинг](#8-проверка-и-мониторинг)
- [Управление приложением](#9-управление-приложением)
- [Откат версии](#10-откат-версии)
- [Troubleshooting](#troubleshooting)

---

## 🎯 Преимущества Dokploy

✅ **Простота**: Развертывание через Web UI за 5-10 минут  
✅ **Автоматизация**: Git push → автоматический rebuild + redeploy  
✅ **Скорость**: Обновление приложения за 30-60 секунд  
✅ **Персистентность**: Данные сохраняются между обновлениями  
✅ **Мониторинг**: Real-time метрики CPU, Memory, Network  
✅ **Откат**: Вернуться к предыдущей версии за 10 секунд  
✅ **Multi-server**: Легко масштабировать на несколько серверов

---

## 1. Подготовка сервера

### Требования

- **ОС**: Ubuntu 24.04 LTS (или 22.04)
- **RAM**: Минимум 2GB (рекомендуется 4GB)
- **CPU**: 1 vCPU (рекомендуется 2+)
- **Диск**: Минимум 10GB свободного места
- **Сеть**: Публичный IP адрес
- **Порты**: 80, 443, 3000 (для Dokploy UI)

### Подключение к серверу

```bash
ssh root@your-server-ip
```

### Обновление системы

```bash
apt update && apt upgrade -y
```

---

## 2. Установка Dokploy

### Быстрая установка (один скрипт)

```bash
curl -sSL https://dokploy.com/install.sh | sh
```

Скрипт автоматически:

- Установит Docker и Docker Compose
- Установит и запустит Dokploy
- Настроит необходимые сервисы (Traefik, PostgreSQL)

**Время установки**: 5-10 минут

### Проверка установки

После установки Dokploy будет доступен по адресу:

```
http://your-server-ip:3000
```

### Первый вход

1. Откройте `http://your-server-ip:3000` в браузере
2. Создайте администраторский аккаунт
3. Войдите в систему

**🔒 Безопасность**: После первого входа рекомендуется настроить HTTPS через Dokploy UI (Settings → SSL)

---

## 3. Подготовка репозитория

### 3.1 Создание GitHub репозитория

Если еще не создан:

```bash
# В директории проекта
git init
git add .
git commit -m "Initial commit: Docker configuration"
git branch -M main
git remote add origin https://github.com/yourusername/tilda-update-checker.git
git push -u origin main
```

### 3.2 Проверка файлов

Убедитесь, что в репозитории есть:

- ✅ `Dockerfile` - конфигурация образа
- ✅ `.dockerignore` - исключения
- ✅ `entrypoint.sh` - скрипт инициализации
- ✅ `docker-compose.yml` - для локального тестирования
- ✅ `env.example` - пример конфигурации
- ✅ `requirements.txt` - зависимости Python

**⚠️ Важно**: НЕ коммитьте файл `.env` с реальными секретами!

### 3.3 Настройка .gitignore

Убедитесь, что `.gitignore` содержит:

```gitignore
.env
.env.local
data/*.db
logs/*.log
venv/
__pycache__/
```

---

## 4. Настройка приложения в Dokploy

### 4.1 Создание нового проекта

1. В Dokploy UI нажмите **"Create Project"**
2. Введите имя проекта: `tilda-update-checker`
3. Описание (опционально): `Мониторинг Tilda CDN изменений`
4. Нажмите **"Create"**

### 4.2 Добавление приложения

1. Внутри проекта нажмите **"Add Application"**
2. Выберите тип: **"Application"** (не Service)
3. Введите имя: `tilda-checker`
4. Выберите **"Git Provider"**

### 4.3 Подключение GitHub

**Способ 1: GitHub App (рекомендуется)**

1. Нажмите **"Connect GitHub"**
2. Авторизуйте Dokploy в GitHub
3. Выберите репозиторий `tilda-update-checker`
4. Выберите ветку: `main`

**Способ 2: SSH/HTTPS URL**

Если GitHub App недоступен:

1. Выберите **"Custom Git URL"**
2. Введите URL: `https://github.com/yourusername/tilda-update-checker.git`
3. Для приватных репозиториев добавьте SSH ключ или Personal Access Token

### 4.4 Build настройки

1. **Build Type**: `Dockerfile`
2. **Dockerfile Path**: `./Dockerfile` (или оставьте пустым для автоопределения)
3. **Build Context**: `.` (корень репозитория)
4. **Build Arguments**: оставьте пустым (не требуется)

---

## 5. Конфигурация volumes

Volumes обеспечивают сохранность данных при перезапуске контейнера.

### 5.1 Создание volumes

В настройках приложения перейдите в раздел **"Volumes"** и добавьте:

#### Volume 1: База данных

- **Name**: `tilda-data`
- **Mount Path (Container)**: `/app/data`
- **Host Path** (опционально): `/var/dokploy/tilda-checker/data`
- **Type**: `bind` (или `volume` для Docker managed)

#### Volume 2: Логи

- **Name**: `tilda-logs`
- **Mount Path (Container)**: `/app/logs`
- **Host Path** (опционально): `/var/dokploy/tilda-checker/logs`
- **Type**: `bind`

### 5.2 Создание директорий на хосте

Если используете bind mounts, создайте директории:

```bash
mkdir -p /var/dokploy/tilda-checker/data
mkdir -p /var/dokploy/tilda-checker/logs
chown -R 1000:1000 /var/dokploy/tilda-checker
```

**Примечание**: UID 1000 соответствует пользователю `tilda` в контейнере

---

## 6. Environment Variables

### 6.1 Минимальная конфигурация

В разделе **"Environment Variables"** добавьте:

```env
# Python настройки
PYTHONUNBUFFERED=1
PYTHONDONTWRITEBYTECODE=1

# Timezone
TZ=Europe/Moscow

# Database
DATABASE_PATH=data/tilda_checker.db

# Logging
LOG_LEVEL=INFO
LOG_FILE=logs/tilda_checker.log

# Monitoring
TILDA_CHECK_INTERVAL=3600
```

### 6.2 Опциональные переменные

#### OpenAI API (для LLM анализа)

```env
OPENAI_API_KEY=sk-your-actual-api-key-here
OPENAI_MODEL=gpt-4o-mini
OPENAI_TEMPERATURE=0.3
OPENAI_MAX_TOKENS=500
```

#### Telegram Bot (для уведомлений)

```env
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHAT_ID=-123456789
```

**Как получить**:

- [OpenAI API Key](https://platform.openai.com/api-keys)
- [Telegram Bot Token](https://t.me/BotFather) - команда `/newbot`

### 6.3 Production настройки

```env
# Автоматическая миграция
AUTO_MIGRATION_ENABLED=true

# Discovery Mode
DISCOVERY_MODE_ENABLED=true

# Порог 404 ошибок
CONSECUTIVE_404_THRESHOLD=3
```

**💡 Совет**: Используйте Dokploy Secrets для чувствительных данных (API ключи)

---

## 7. Автоматический деплой

### 7.1 Включение Auto-Deploy

В настройках приложения:

1. Перейдите в **"Deployment"** → **"Auto Deploy"**
2. Включите **"Enable Auto Deploy on Push"**
3. Выберите ветку: `main`
4. Сохраните

### 7.2 Настройка Webhook (опционально)

Dokploy автоматически настраивает webhook в GitHub.

Проверить можно в:

```
GitHub → Repository → Settings → Webhooks
```

### 7.3 Первый деплой

Нажмите кнопку **"Deploy"** в Dokploy UI.

**Процесс деплоя**:

1. ✅ Git clone репозитория
2. ✅ Docker build образа (2-5 минут при первой сборке)
3. ✅ Создание volumes
4. ✅ Запуск контейнера
5. ✅ Healthcheck проверка

**Время**: 3-7 минут для первого деплоя

### 7.4 Последующие деплои

После настройки auto-deploy:

```bash
# Внесите изменения в код
git add .
git commit -m "Update: feature X"
git push origin main

# Dokploy автоматически:
# - Получит уведомление от GitHub
# - Запустит rebuild (30-60 сек благодаря кэшу)
# - Остановит старый контейнер (graceful shutdown)
# - Запустит новый контейнер
# - Данные сохранятся (volumes)
```

---

## 8. Проверка и мониторинг

### 8.1 Проверка статуса

В Dokploy UI:

**Dashboard** → Ваше приложение → **Status**

Индикаторы:

- 🟢 **Running** - работает нормально
- 🟡 **Starting** - запускается
- 🔴 **Stopped** - остановлено
- ⚠️ **Unhealthy** - healthcheck failed

### 8.2 Просмотр логов

**Real-time логи**:

1. В Dokploy UI откройте приложение
2. Перейдите в **"Logs"**
3. Выберите **"Live Logs"**

**Логи на хосте**:

```bash
# Логи контейнера
docker logs tilda-update-checker -f

# Логи приложения (если volume настроен)
tail -f /var/dokploy/tilda-checker/logs/tilda_checker.log
```

### 8.3 Мониторинг ресурсов

В Dokploy UI → **Monitoring**:

- **CPU Usage**: текущая нагрузка
- **Memory Usage**: использование RAM
- **Network I/O**: входящий/исходящий трафик
- **Disk I/O**: операции чтения/записи

### 8.4 Healthcheck

Dokploy использует healthcheck из Dockerfile:

```bash
# Проверка каждые 30 секунд
# Условие: наличие файла data/tilda_checker.db
```

Если healthcheck падает 3 раза подряд - контейнер помечается как **Unhealthy**.

---

## 9. Управление приложением

### 9.1 Запуск/Остановка

**Через Dokploy UI**:

- **Start**: Кнопка ▶️ Play
- **Stop**: Кнопка ⏹️ Stop
- **Restart**: Кнопка 🔄 Restart

**Через командную строку**:

```bash
# Остановить
docker stop tilda-update-checker

# Запустить
docker start tilda-update-checker

# Перезапустить
docker restart tilda-update-checker
```

### 9.2 Выполнение команд внутри контейнера

**Через Dokploy UI**:

1. Откройте приложение
2. Перейдите в **"Terminal"**
3. Выполните команду

**Через командную строку**:

```bash
# Подключиться к shell
docker exec -it tilda-update-checker bash

# Выполнить CLI команду
docker exec tilda-update-checker python main.py --show-announcements

# Проверить Discovery Mode
docker exec tilda-update-checker python main.py --discover

# Показать dashboard
docker exec tilda-update-checker python main.py --dashboard
```

### 9.3 Доступ к базе данных

```bash
# Копировать БД на хост
docker cp tilda-update-checker:/app/data/tilda_checker.db ./backup.db

# Или через volume
cp /var/dokploy/tilda-checker/data/tilda_checker.db ./backup.db
```

### 9.4 Backup данных

**Автоматический backup** (через cron на хосте):

```bash
# Создать скрипт backup
cat > /usr/local/bin/backup-tilda.sh << 'EOF'
#!/bin/bash
BACKUP_DIR="/var/backups/tilda-checker"
DATE=$(date +%Y%m%d_%H%M%S)

mkdir -p $BACKUP_DIR

# Backup БД
cp /var/dokploy/tilda-checker/data/tilda_checker.db \
   $BACKUP_DIR/tilda_checker_${DATE}.db

# Удалить старые backup (старше 30 дней)
find $BACKUP_DIR -name "*.db" -mtime +30 -delete

echo "Backup completed: tilda_checker_${DATE}.db"
EOF

chmod +x /usr/local/bin/backup-tilda.sh

# Добавить в cron (ежедневно в 2:00)
echo "0 2 * * * /usr/local/bin/backup-tilda.sh" | crontab -
```

---

## 10. Откат версии

### 10.1 Через Dokploy UI

1. Откройте приложение
2. Перейдите в **"Deployments"** → **"History"**
3. Найдите нужную версию
4. Нажмите **"Rollback"**

**Время отката**: 10-30 секунд

### 10.2 Через Git

```bash
# Откатить к предыдущему коммиту
git revert HEAD
git push origin main

# Или к конкретному коммиту
git revert abc123def
git push origin main

# Dokploy автоматически задеплоит откат
```

### 10.3 Восстановление данных

Если нужно восстановить старую БД:

```bash
# Остановить контейнер
docker stop tilda-update-checker

# Восстановить backup
cp /var/backups/tilda-checker/tilda_checker_20250101_020000.db \
   /var/dokploy/tilda-checker/data/tilda_checker.db

# Запустить контейнер
docker start tilda-update-checker
```

---

## Troubleshooting

### Проблема: Контейнер не запускается

**Диагностика**:

```bash
# Проверить логи
docker logs tilda-update-checker

# Проверить статус
docker ps -a | grep tilda

# Проверить образ
docker images | grep tilda
```

**Возможные причины**:

1. **Ошибка в Dockerfile** - проверьте синтаксис
2. **Проблемы с правами** - проверьте права на volumes
3. **Недостаточно ресурсов** - увеличьте RAM/CPU limits

**Решение**:

```bash
# Пересоздать контейнер
docker rm -f tilda-update-checker
# Задеплоить заново через Dokploy UI
```

### Проблема: База данных не сохраняется

**Проверка volumes**:

```bash
docker inspect tilda-update-checker | grep -A 10 "Mounts"
```

**Решение**:

1. Проверьте настройки volumes в Dokploy
2. Убедитесь, что директория существует на хосте
3. Проверьте права доступа: `chown -R 1000:1000 /var/dokploy/tilda-checker`

### Проблема: Healthcheck failed

**Диагностика**:

```bash
# Проверить healthcheck
docker inspect tilda-update-checker | grep -A 10 "Health"

# Выполнить healthcheck вручную
docker exec tilda-update-checker python -c "from pathlib import Path; print(Path('data/tilda_checker.db').exists())"
```

**Решение**:

1. Проверьте, что БД инициализирована
2. Проверьте права на `/app/data`
3. Увеличьте `start_period` в Dockerfile если инициализация долгая

### Проблема: Auto-deploy не работает

**Проверка webhook**:

1. GitHub → Repository → Settings → Webhooks
2. Проверьте статус последних доставок
3. Должен быть webhook на `https://your-dokploy-domain/api/...`

**Решение**:

1. Переподключите GitHub в Dokploy
2. Проверьте, что Dokploy доступен извне
3. Проверьте настройки Auto-Deploy в приложении

### Проблема: Медленная сборка

**Оптимизация**:

1. Проверьте `.dockerignore` - исключите ненужные файлы
2. Используйте Docker layer caching
3. Увеличьте ресурсы сервера

**Время сборки**:

- Первая сборка: 3-5 минут
- Последующие (с кэшем): 30-60 секунд

### Проблема: Нет логов в Dokploy UI

**Решение**:

1. Проверьте, что `PYTHONUNBUFFERED=1` установлен
2. Перезапустите контейнер
3. Проверьте логи напрямую: `docker logs tilda-update-checker`

---

## 📊 Метрики производительности

После настройки ожидайте:

| Метрика                | Значение                           |
| ---------------------- | ---------------------------------- |
| **Первый деплой**      | 3-7 минут                          |
| **Последующие деплои** | 30-60 секунд                       |
| **Время отката**       | 10-30 секунд                       |
| **Размер образа**      | ~150-200MB                         |
| **Использование RAM**  | ~100-150MB (idle)                  |
| **Использование CPU**  | <5% (idle), ~20-30% (при проверке) |

---

## 🎓 Дополнительные ресурсы

- **Dokploy Документация**: https://dokploy.com/docs
- **Dokploy GitHub**: https://github.com/Dokploy/dokploy
- **Dokploy Discord**: https://discord.gg/dokploy
- **Docker Best Practices**: https://docs.docker.com/develop/dev-best-practices/

---

## ✅ Checklist готовности к production

Перед запуском в production проверьте:

- [ ] ✅ Dokploy установлен и настроен HTTPS
- [ ] ✅ GitHub репозиторий настроен с правильным `.gitignore`
- [ ] ✅ Volumes настроены для `/app/data` и `/app/logs`
- [ ] ✅ Environment variables заполнены (как минимум базовые)
- [ ] ✅ OPENAI_API_KEY добавлен (для LLM анализа)
- [ ] ✅ TELEGRAM_BOT_TOKEN добавлен (для уведомлений)
- [ ] ✅ Auto-deploy включен
- [ ] ✅ Healthcheck работает корректно
- [ ] ✅ Первый деплой успешно завершен
- [ ] ✅ Приложение запустилось и работает (проверьте логи)
- [ ] ✅ База данных создана и доступна
- [ ] ✅ Backup настроен (cron job)
- [ ] ✅ Мониторинг настроен в Dokploy
- [ ] ✅ Проверена автоматическая проверка (подождите интервал)

---

## 🎉 Готово!

Ваше приложение **Tilda Update Checker** успешно развернуто и настроено автоматическое обновление!

**Workflow после настройки**:

```
1. Разрабатываете новую функцию локально
2. Тестируете через docker-compose up
3. git push origin main
4. Dokploy автоматически деплоит за 30-60 сек
5. Данные и логи сохраняются
6. Мониторинг в реальном времени
```

**Наслаждайтесь автоматизированным деплоем! 🚀**
